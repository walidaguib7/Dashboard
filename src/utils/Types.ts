export type LoginType = {
    username: string;
    password: string;
}

export type SignUpType = {
    username: string;
    email: string;
    password: string;
}

export type ServiceType = {
    id: number;
    title: string;
    description: string;
}